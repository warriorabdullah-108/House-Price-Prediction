# Pin these to whatever `python -c "import sklearn; print(sklearn.__version__)"`
# prints in YOUR notebook environment — a pickle only loads reliably with the
# same scikit-learn version it was trained with.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
pandas==2.2.2
scikit-learn==1.6.1
joblib==1.4.2
pytest==8.3.3
httpx==0.27.2
